---
galaxy_info:
  author: Jacopo Secchiero
  description: Playbook for PostgreSQL.
  company: Enter srl
  license: MIT
  min_ansible_version: 2.4
  platforms:
    - name: Ubuntu
      versions:
        - xenial
  galaxy_tags:
    - molecule
    - postgres
    - database

dependencies:
  - { role: geerlingguy.postgresql, version: 1.3.1 }
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
